---
import Layout from '../layouts/Layout.astro';
import RopeDivider from '../components/RopeDivider.astro';
import { getSiteConfig } from '../lib/supabase';

const config = await getSiteConfig();
const headline = config.home_headline || 'Amigos que escalam juntos';
const fotoCapa = config.home_foto_capa || '/hero-capa.jpg';

const destaques = [
  { titulo: 'Sobre', texto: 'Quem são os Climbeiros.', href: '/sobre', cor: 'var(--creme)', icone: 'sobre' },
  { titulo: 'Beta', texto: 'Do 101 até o Crux, pra quem já escala.', href: '/beta', cor: 'var(--laranja)', icone: 'beta' },
  { titulo: 'Academias', texto: 'Onde treinar e onde aprender.', href: '/academias', cor: 'var(--amarelo)', icone: 'academias' },
  { titulo: 'Guias & Cursos', texto: 'Cursos e conteúdo pra evoluir.', href: '/cursos', cor: 'var(--creme)', icone: 'guias' },
  { titulo: 'Rotas & Setores', texto: 'Encontre vias e croquis pelo Brasil.', href: '/rotas', cor: 'var(--laranja)', icone: 'rotas' },
  { titulo: 'Cupons & Parcerias', texto: 'Descontos com marcas parceiras.', href: '/parceiros', cor: 'var(--amarelo)', icone: 'cupons' },
  { titulo: 'Galeria', texto: 'Fotos da comunidade escalando.', href: '/galeria', cor: 'var(--creme)', icone: 'galeria' },
  { titulo: 'Contato', texto: 'Fale com a gente.', href: '/contato', cor: 'var(--laranja)', icone: 'contato' },
];
---
<Layout titulo="Início">
  <section class="hero">
    <div class="hero__imagem">
      <img src={fotoCapa} alt="Escalador em uma via de escalada" />
      <div class="hero__sombra"></div>
    </div>
    <div class="container hero__conteudo">
      <p class="eyebrow hero__eyebrow">Comunidade de escalada no Brasil</p>
      <h1 class="hero__titulo">{headline}</h1>
      <div class="hero__acoes">
        <a href="/rotas" class="botao">Ver rotas & setores</a>
        <a href="/beta" class="botao botao--secundario" style="border-color: var(--creme-claro); color: var(--creme-claro);">Ver o Beta</a>
      </div>
    </div>
  </section>

  <section class="secao">
    <div class="container">
      <div class="grade destaques-grade">
        {destaques.map((d) => (
          <a href={d.href} class="destaque-card" style={`--cor-destaque: ${d.cor}`}>
            <div class="destaque-card__topo">
              <span class="destaque-card__icone">
                {d.icone === 'sobre' && (
                  <svg viewBox="0 0 40 40">
                    <circle cx="15" cy="16" r="7" fill="#2a1f16" opacity="0.85" />
                    <circle cx="25" cy="16" r="7" fill="#2a1f16" />
                    <path d="M6 33c0-6 5-10 9-10s9 4 9 10" fill="#2a1f16" opacity="0.85" />
                    <path d="M16 33c0-6 5-10 9-10s9 4 9 10" fill="#2a1f16" />
                  </svg>
                )}
                {d.icone === 'beta' && (
                  <svg viewBox="0 0 40 40">
                    <line x1="14" y1="6" x2="14" y2="34" stroke="#2a1f16" stroke-width="3" stroke-linecap="round" />
                    <path d="M14 8 L32 14 L14 20 Z" fill="#2a1f16" />
                  </svg>
                )}
                {d.icone === 'academias' && (
                  <svg viewBox="0 0 40 40">
                    <polygon points="20,6 32,12 32,30 24,34 24,18" fill="#2a1f16" opacity="0.75" />
                    <polygon points="20,6 8,30 24,34 24,18" fill="#2a1f16" />
                    <circle cx="20" cy="24" r="1.6" fill="#e7d18e" />
                  </svg>
                )}
                {d.icone === 'guias' && (
                  <svg viewBox="0 0 40 40">
                    <path d="M20 11 C16 8 10 8 6 10 V30 C10 28 16 28 20 31 C24 28 30 28 34 30 V10 C30 8 24 8 20 11 Z" fill="#2a1f16" />
                    <line x1="20" y1="11" x2="20" y2="31" stroke="#e7d18e" stroke-width="1.4" />
                  </svg>
                )}
                {d.icone === 'rotas' && (
                  <svg viewBox="0 0 40 40">
                    <polygon points="6,30 16,12 22,22 27,15 34,30" fill="#2a1f16" opacity="0.85" />
                    <path d="M28 6c-3 0-5 2.3-5 5 0 3.6 5 9 5 9s5-5.4 5-9c0-2.7-2-5-5-5z" fill="#da7326" />
                    <circle cx="28" cy="11" r="1.8" fill="#2a1f16" />
                  </svg>
                )}
                {d.icone === 'cupons' && (
                  <svg viewBox="0 0 40 40">
                    <path d="M6 18 L18 6 H32 a2 2 0 0 1 2 2 V22 L22 34 a3 3 0 0 1 -4.2 0 L6 22.2 a3 3 0 0 1 0 -4.2 Z" fill="#2a1f16" />
                    <circle cx="26" cy="14" r="2.6" fill="#e7d18e" />
                  </svg>
                )}
                {d.icone === 'galeria' && (
                  <svg viewBox="0 0 40 40">
                    <rect x="5" y="8" width="30" height="24" rx="3" fill="none" stroke="#2a1f16" stroke-width="2.6" />
                    <circle cx="14" cy="16" r="3" fill="#2a1f16" />
                    <path d="M8 28 L17 18 L23 24 L27 20 L33 28 Z" fill="#2a1f16" />
                  </svg>
                )}
                {d.icone === 'contato' && (
                  <svg viewBox="0 0 40 40">
                    <path d="M6 10 h28 a3 3 0 0 1 3 3 v11 a3 3 0 0 1 -3 3 h-16 l-7 6 v-6 h-5 a3 3 0 0 1 -3 -3 v-11 a3 3 0 0 1 3 -3 z" fill="#2a1f16" />
                    <circle cx="15" cy="19" r="1.8" fill="#e7d18e" />
                    <circle cx="20" cy="19" r="1.8" fill="#e7d18e" />
                    <circle cx="25" cy="19" r="1.8" fill="#e7d18e" />
                  </svg>
                )}
              </span>
              <h3>{d.titulo}</h3>
            </div>
            <p>{d.texto}</p>
            <span class="destaque-card__seta">
              <svg viewBox="0 0 24 24">
                <path d="M6 18 L18 6 M18 6 H9 M18 6 V15" stroke="#2a1f16" stroke-width="2.4" fill="none" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <RopeDivider />

  <section class="secao secao--verde">
    <div class="container secao-cta">
      <h2>Sua academia ou curso ainda não está aqui?</h2>
      <p>O Climbeiros é feito pela e para a comunidade. Fale com a gente e vamos incluir.</p>
      <a href="/contato" class="botao">Entrar em contato</a>
    </div>
  </section>
</Layout>

<style>
  .hero {
    position: relative;
    min-height: 480px;
    display: flex;
    align-items: flex-end;
    color: var(--branco);
  }
  .hero__imagem {
    position: absolute;
    inset: 0;
  }
  .hero__imagem img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 70%;
  }
  .hero__sombra {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(42,31,22,0.15) 0%, rgba(42,31,22,0.85) 100%);
  }
  .hero__conteudo {
    position: relative;
    padding-bottom: 56px;
    padding-top: 200px;
  }
  .hero__eyebrow { color: var(--branco); }
  .hero__eyebrow::before { background: var(--branco); }

  .hero__titulo {
    color: var(--branco);
    max-width: 680px;
  }
  .hero__acoes {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-top: 24px;
  }

  /* Cartões de destaque — fundo branco com borda colorida no topo (uma cor
     por card, alternando creme/laranja/amarelo), ícone ilustrado, título,
     descrição e uma setinha discreta. Grade fixa de 4 colunas (duas linhas
     de 4, já que são 8 itens). */
  .destaques-grade {
    grid-template-columns: repeat(4, 1fr);
  }
  @media (max-width: 780px) {
    .destaques-grade { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 420px) {
    .destaques-grade { grid-template-columns: 1fr; }
  }

  .destaque-card {
    display: flex;
    flex-direction: column;
    background: var(--branco);
    border-radius: var(--raio);
    border-top: 4px solid var(--cor-destaque, var(--laranja));
    box-shadow: var(--sombra);
    padding: 14px 16px;
    min-height: 128px;
    transition: transform 0.15s ease;
  }
  .destaque-card:hover {
    transform: translateY(-3px);
    text-decoration: none;
  }
  .destaque-card__topo {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .destaque-card__icone {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--creme-claro);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .destaque-card__icone svg {
    width: 22px;
    height: 22px;
    overflow: visible;
  }
  .destaque-card h3 {
    font-family: var(--fonte-display);
    text-transform: uppercase;
    letter-spacing: 0.01em;
    color: var(--verde-escuro);
    font-size: 0.9rem;
    line-height: 1.2;
    margin: 0;
  }
  .destaque-card p {
    color: var(--marrom);
    opacity: 0.82;
    font-size: 0.8rem;
    line-height: 1.35;
    margin: 8px 0 0;
  }
  .destaque-card__seta {
    margin-top: auto;
    align-self: flex-end;
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: var(--creme-claro);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .destaque-card__seta svg {
    width: 13px;
    height: 13px;
  }

  .secao-cta {
    text-align: center;
  }
  .secao-cta p { max-width: 520px; margin: 0 auto 24px; opacity: 0.9; }
</style>
